public class Collisions {
    //if 2 hash values generate same index collision occurs
    //we can avoid those by either chaining or open addressing

    //Chaining
    //In chaining,instead of storing hash value in array,we store it in linked list
    //each cell in array will be pointing a linked list
    //so when collsion occurs the next hash value will be added to end of linked list which is pointed by same array cell

    //open addressing
    //storing the hash value in a different address in the array
    
}
